<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Background color -->
    <solid android:color="#ead1dc" /> <!-- Change this to your desired background color -->
    <!-- Border color and width -->
    <!-- Corner radius for rounded edges -->
    <corners android:radius="8dp" /> <!-- Change the radius value as needed for desired roundness -->
    <!-- Padding for the text inside the button -->
    <padding
        android:left="14dp"
        android:right="14dp"
        android:top="14dp"
        android:bottom="14dp" />
</shape>
